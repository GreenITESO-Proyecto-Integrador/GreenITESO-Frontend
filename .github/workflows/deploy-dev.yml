name: Deploy - dev

on:
  push:
    branches: [dev]

jobs:
  deploy:
    if: vars.CLOUD_DEPLOYMENT_ENABLED == 'true'
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: dev
      build: true
    secrets: inherit
