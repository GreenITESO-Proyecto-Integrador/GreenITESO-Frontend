name: Deploy - prod

on:
  push:
    branches: [prod]

jobs:
  deploy:
    if: vars.CLOUD_DEPLOYMENT_ENABLED == 'true'
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: prod
      build: false
    secrets: inherit
