name: Deploy - preprod

on:
  push:
    branches: [preprod]

jobs:
  deploy:
    if: vars.CLOUD_DEPLOYMENT_ENABLED == 'true'
    permissions:
      contents: read
      id-token: write
    uses: ./.github/workflows/_deploy.yml
    with:
      environment: preprod
      build: false
    secrets: inherit
